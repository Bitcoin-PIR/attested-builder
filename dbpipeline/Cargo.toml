[package]
name = "dbpipeline"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Deterministic BitcoinPIR database build pipeline stages"

[dependencies]
hex = "=0.4.3"
sha2 = { version = "=0.10.9", default-features = false }
utxosnapshot = { path = "../utxosnapshot" }
